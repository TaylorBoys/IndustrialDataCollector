<Window x:Class="WpfApp.DataPointWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="数据点管理" Height="650" Width="950"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource ModernWindow}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="数据点列表" FontWeight="Bold" FontSize="20" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="添加数据点" Click="AddButton_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,12,0"/>
            <Button Content="编辑数据点" Click="EditButton_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,12,0"/>
            <Button Content="删除数据点" Click="DeleteButton_Click" Style="{StaticResource DangerButton}"/>
        </StackPanel>
        
        <DataGrid Grid.Row="2" x:Name="DataPointListView"
                  Style="{StaticResource ModernDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="160"/>
                <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="130"/>
                <DataGridTextColumn Header="数据类型" Binding="{Binding DataType, Converter={StaticResource DataTypeToChineseConverter}}" Width="140"/>
                <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="100"/>
                <DataGridTextColumn Header="缩放因子" Binding="{Binding ScaleFactor}" Width="120"/>
                <DataGridTextColumn Header="偏移量" Binding="{Binding Offset}" Width="100"/>
                <DataGridTextColumn Header="可写" Binding="{Binding IsWritable}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="取消" Click="CancelButton_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,12,0"/>
            <Button Content="确定" Click="OKButton_Click" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
